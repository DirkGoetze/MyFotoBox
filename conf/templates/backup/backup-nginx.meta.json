{
    "timestamp": "${timestamp}",
    "source": "${source}",
    "backup": "${backup}",
    "config_type": "${config_type}",
    "action": "${action}",
    "version": "1.0",
    "description": "Backup der NGINX-Konfiguration"
}
